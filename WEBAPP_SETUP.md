# Настройка Web App для https://finadvisor-ai.ru

## Выбор фреймворка

У вас есть два варианта:

### Вариант 1: FastAPI (рекомендуется) ✅
- **Файл:** `api.py` 
- **Запуск:** `python api.py` или `uvicorn api:app --host 0.0.0.0 --port 8000`
- **Преимущества:** Асинхронный, быстрый, современный
- **Зависимости:** fastapi, uvicorn (уже в requirements.txt)

### Вариант 2: Flask
- **Файл:** `app.py` (ваш существующий)
- **Запуск:** `python app.py` или `gunicorn app:app`
- **Преимущества:** Простой, знакомый многим
- **Зависимости:** flask, gunicorn (нужно добавить в requirements.txt)

## Рекомендация

**Используйте FastAPI (`api.py`)** потому что:
1. ✅ Бот уже использует асинхронный код (asyncpg, aiogram)
2. ✅ FastAPI лучше работает с async/await
3. ✅ API уже создан и протестирован
4. ✅ Зависимости уже установлены

## Настройка для вашего домена

### 1. Обновите `.env`:
```
WEB_APP_URL=https://finadvisor-ai.ru
```

### 2. Запустите API сервер:
```bash
# Вариант 1: FastAPI (рекомендуется)
python api.py

# Или через uvicorn
uvicorn api:app --host 0.0.0.0 --port 8000
```

### 3. Настройте nginx (если используете):
```nginx
server {
    listen 80;
    server_name finadvisor-ai.ru;
    
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 4. Настройте SSL (обязательно для Telegram):
```bash
# Используйте certbot или ваш провайдер SSL
certbot --nginx -d finadvisor-ai.ru
```

### 5. В BotFather:
- Откройте настройки бота
- Выберите "Bot Settings" → "Menu Button"
- Укажите URL: `https://finadvisor-ai.ru`

## Если хотите использовать Flask

Если вы предпочитаете Flask, нужно:

1. **Добавить в requirements.txt:**
```
flask
gunicorn
```

2. **Обновить app.py** - добавить все API endpoints из api.py

3. **Запуск:**
```bash
gunicorn -w 4 -b 0.0.0.0:8000 app:app
```

Но это потребует переписывания всех endpoints на Flask синтаксис.

## Проверка работы

1. Откройте в браузере: `https://finadvisor-ai.ru`
2. Должна открыться главная страница Web App
3. В Telegram нажмите кнопку "🌐 Открыть Web App"
4. Проверьте работу всех функций

## Важно

- ✅ URL должен быть доступен по HTTPS (обязательно для Telegram)
- ✅ Домен должен быть настроен в BotFather
- ✅ API сервер должен быть запущен и доступен
- ✅ База данных должна быть доступна с сервера
